<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.laodeng.crm.workbench.dao.CustomerRemarkDao">

    <insert id="save" parameterType="CustomerRemark">

        insert into tbl_customer_remark(

            id,
            note_content,
            create_by,
            create_time,
            edit_by,
            edit_time,
            edit_flag,
            customer_id

        )values(

            #{id},
            #{note_content},
            #{create_by},
            #{create_time},
            #{edit_by},
            #{edit_time},
            #{edit_flag},
            #{customer_id}

        )

    </insert>

</mapper>